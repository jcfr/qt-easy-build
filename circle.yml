machine:
  services:
    - docker

dependencies:
  override:
    - docker info
    - docker pull ubuntu
    - ~/qt-easy-build/Testing/Docker/build.sh

test:
  override:
    - ~/qt-easy-build/Testing/circleCITest.sh

